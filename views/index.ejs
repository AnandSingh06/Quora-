<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body style="background-color: rgb(163, 220, 201);">
    <h1>Quora Posts</h1>
<% for(post of posts){ %>
  <div class="post">
    <h3 class="user">@<%= post.username %></h3>
    <p class="content"><%= post.content %></p>
    <div class="btn-container">
      <a href="/post/<%= post.id %>">
      <button>See in Detail</button>
    </a>
    <a href="/post/<%= post.id %>/edit">
      <button>Edit</button>
    </a>
    <form method="post" action="/post/<%=post.id%>?_method=DELETE">
      <button>Delete post</button>
    </form>
    
    </div> 
  </div>
<% } %>

<a href="/post/new"><button>Create a New Post</button></a>

</body>
</html>